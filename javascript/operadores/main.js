// Operadores e Expressões
const soma = 1 + 1;
const subtracao = 1 - 1;
const multiplicacao = 1 - 1;
const divisao = 1 / 1;
const resto = 1 % 1;
const expoente = 1 ** 1;
const e = 2e10;

// agrupamento
const agrupamento = (5 + 5) / 2;

// atribuição
const atribuicao = 2;
atribuicao += 4;

// incremento e decremento
const numero = 5;
numero++;
--numero;

// transformar em número
const quantidade = "20";
console.log(+quantidade);

// a ordem importa * / + -
